{
  "name": "@semantic-diff-tracer/tui",
  "version": "0.0.3",
  "private": true,
  "type": "module",
  "bin": {
    "semantic-diff-tracer": "./dist/main.js"
  },
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -b",
    "bundle": "tsc -b && node esbuild.mjs",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@semantic-diff-tracer/core": "*",
    "@semantic-diff-tracer/diff-git": "*",
    "@semantic-diff-tracer/github-octokit": "*",
    "@semantic-diff-tracer/llm-claude": "*",
    "@semantic-diff-tracer/session-store-fs": "*"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
